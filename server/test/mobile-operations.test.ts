import test from'node:test';import assert from'node:assert/strict';import{openDb}from'../src/db.ts';import{mobileOperationsHealth}from'../src/mobileOperations.ts'
test('mobile operations health exposes counts and alerts without business payloads',()=>{const db=openDb(':memory:');db.prepare(`INSERT INTO mobile_submissions(client_submission_id,receipt_id,round_id,owner_id,device_id,task_version,rule_version,draft_revision,canonical_payload,payload_hash,attachment_receipts,status,created_at,updated_at)VALUES('s','r','round','u','d','t','rule',1,'secret-payload','h','[]','finalizing','2026-08-17T00:00:00.000Z','2026-08-17T00:00:00.000Z')`).run();db.prepare(`INSERT INTO staged_attachments(receipt_id,round_id,sample_slot_id,client_attachment_id,owner_id,device_id,content_hash,size,mime,revision,status,lease_expires_at,generation,created_at,updated_at)VALUES('a','round','slot','photo','u','d','h',1,'image/jpeg',1,'storage_error','2026-08-17',1,'2026-08-17','2026-08-17')`).run();const result=mobileOperationsHealth(db,Date.parse('2026-08-17T01:00:00.000Z'));assert.equal(result.alerts.length,2);assert.equal(JSON.stringify(result).includes('secret-payload'),false);assert.deepEqual(result.counts,{pending:0,finalizing:1,complete:0,attachmentStorageErrors:1})})
